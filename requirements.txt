# Dependências de execução do ScreenShare
#
# As versões usam ">=" para permitir instalação em Python 3.9 até 3.14.

mss>=9.0.1
opencv-python>=4.8
numpy>=1.24
pillow>=10.0

# Áudio: opcional, porém recomendado.
# O sounddevice é o motor preferencial por ter rodas compatíveis com o
# Python 3.13/3.14 e depender apenas do PortAudio do sistema.
#   Windows/macOS: nada além do pip é necessário.
#   Linux (Debian/Ubuntu):  sudo apt install libportaudio2
# Se o sounddevice não estiver presente, o projeto tenta o PyAudio
# automaticamente; sem nenhum dos dois, roda apenas com vídeo e chat.
sounddevice>=0.4.6

# WebRTC (modo internet) — opcional nesta fase.
# Instale para ativar o modo de conexão pela internet com NAT traversal.
#   pip install aiortc aiohttp
aiortc>=1.9.0
aiohttp>=3.9.0
